# how to use pros conductor:

## in the library:
change the version to a new version
run: `pros make template`

copy template zip to destination project

## in the destination project:
run: `pros conduct fetch (filename.zip)` to add package to package list
run: `pros conduct apply (library-name)` to update/install package
